<%@ Master Language="C#" AutoEventWireup="true" CodeBehind="Donar.Master.cs" Inherits="BBMS.Donar" %>

<!DOCTYPE html>

<html>
<head runat="server">
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    
    <title>Donor Portal</title>
    
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Bootstrap Icons CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

    <asp:ContentPlaceHolder ID="head" runat="server">
    </asp:ContentPlaceHolder>

    <style type="text/css">
        * { box-sizing: border-box; }
        body { background:#f8fafc; font-family:'Segoe UI',sans-serif; margin:0; color:#1e293b; }
        .dash-wrapper { display:flex; min-height:100vh; }

        /* Donor Sidebar Color */
        .sidebar { width:260px; min-height:100vh; padding:20px 0; color:#fff; flex-shrink:0; background:#3a1f5d; }

        .sidebar-brand { display:flex; align-items:center; gap:12px; padding:0 22px 22px 22px; border-bottom:1px solid rgba(255,255,255,.12); margin-bottom:15px; }
        .sidebar-brand .drop { background:#e11d2e; width:40px; height:40px; border-radius:10px; display:flex; align-items:center; justify-content:center; font-size:20px; color:#fff; }
        .sidebar-brand h5 { margin:0; font-weight:700; font-size:16px; color:#fff; }
        .sidebar-brand small { opacity:.7; letter-spacing:.5px; font-size:11.5px; }

        .sidebar-menu { list-style:none; padding:0 12px; margin:0; }
        .sidebar-menu li { list-style:none; }
        .sidebar-menu li a { display:flex; align-items:center; gap:12px; padding:12px 14px; color:#d7dbe6; text-decoration:none; border-radius:8px; margin-bottom:4px; font-size:14.5px; font-weight:500; transition:.2s; }
        .sidebar-menu li a:hover { background:rgba(255,255,255,.08); color:#fff; }
        .sidebar-menu li a.active { color:#fff; font-weight:600; background:rgba(255,255,255,.15); }

        .main-content { flex:1; padding:28px 32px; background:#f8fafc; }
        .topbar { display:flex; justify-content:space-between; align-items:center; margin-bottom:26px; }
        .topbar h3 { margin:0; font-weight:800; color:#3a1f5d; font-size:24px; }
        .topbar .profile-box { display:flex; align-items:center; gap:10px; background:#fff; padding:6px 14px 6px 6px; border-radius:30px; box-shadow:0 2px 8px rgba(0,0,0,.04); border:1px solid #e2e8f0; }
        .topbar .profile-box img { width:34px; height:34px; border-radius:50%; object-fit:cover; }

        .stats-row { display:grid; grid-template-columns:repeat(auto-fit,minmax(190px,1fr)); gap:18px; margin-bottom:26px; }
        .stat-card { background:#fff; border:1px solid #e2e8f0; border-radius:14px; padding:18px; display:flex; align-items:flex-start; gap:14px; box-shadow:0 2px 6px rgba(0,0,0,.02); opacity:0; transform:translateY(15px); animation:fadeUp .5s ease forwards; }
        .stats-row .stat-card:nth-child(1){animation-delay:.05s}
        .stats-row .stat-card:nth-child(2){animation-delay:.15s}
        .stats-row .stat-card:nth-child(3){animation-delay:.25s}
        .stats-row .stat-card:nth-child(4){animation-delay:.35s}
        .stats-row .stat-card:nth-child(5){animation-delay:.45s}

        @keyframes fadeUp { to { opacity:1; transform:translateY(0); } }

        .stat-icon { width:46px; height:46px; border-radius:10px; display:flex; align-items:center; justify-content:center; font-size:20px; flex-shrink:0; }
        .icon-blue   { background:#eff6ff; color:#2563eb; }
        .icon-green  { background:#f0fdf4; color:#16a34a; }
        .icon-red    { background:#fef2f2; color:#e11d2e; }
        .icon-orange { background:#fffbeb; color:#d97706; }
        .icon-purple { background:#f5f3ff; color:#7c3aed; }

        .stat-card h4 { margin:0; font-size:22px; font-weight:800; color:#1e293b; }
        .stat-card span.label { font-size:13px; color:#64748b; }
        .stat-card a.link { display:block; font-size:12.5px; font-weight:600; margin-top:6px; text-decoration:none; color:#e11d2e; }

        .qa-title { font-weight:700; font-size:17px; margin-bottom:14px; color:#1e293b; }
        .quick-access { display:grid; grid-template-columns:repeat(auto-fit,minmax(170px,1fr)); gap:16px; }
        .qa-card { background:#fff; border:1px solid #e2e8f0; border-radius:12px; padding:22px 10px; text-align:center; text-decoration:none; color:#1e293b; font-weight:600; font-size:14px; transition:transform .25s ease, box-shadow .25s ease, border-color .25s ease; }
        .qa-card i { display:block; font-size:24px; margin-bottom:10px; color:#e11d2e; }
        .qa-card:hover { transform:translateY(-4px); box-shadow:0 8px 16px rgba(0,0,0,.06); border-color:#3a1f5d; }

        .badge { padding:4px 10px; border-radius:20px; font-size:12px; font-weight:600; display:inline-block; }
        .badge-group    { background:#fef2f2; color:#e11d2e; font-weight:700; }
        .badge-approved { background:#f0fdf4; color:#16a34a; }
        .badge-rejected { background:#fef2f2; color:#e11d2e; }
        .badge-pending  { background:#fffbeb; color:#d97706; }
    </style>
</head>
<body>
    <form id="form1" runat="server">
        <div>
            <asp:ContentPlaceHolder ID="MainContent" runat="server">
            </asp:ContentPlaceHolder>
        </div>
    </form>
</body>
</html>